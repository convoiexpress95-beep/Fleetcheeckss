// Fichier neutralisé : ancien wrapper embed MissionsModern.
export default function MissionsModernEmbed() {
  if (import.meta.env.DEV) console.warn('[MissionsModernEmbed] supprimé. Utilisez /missions');
  return null;
}
