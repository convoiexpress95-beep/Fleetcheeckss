// Fichier neutralisé: renommé virtuellement via pattern ignore (aucun test).
// Pour éviter l'exécution par Vitest, ajouter dans config un exclude.
export const __disabled_route_missions_modern = true;
